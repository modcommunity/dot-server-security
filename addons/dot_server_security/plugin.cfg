[plugin]

name="dot-server-security"
description="Server hardening as configuration: sliding-window rules over chat, connections, authentication and RCON, escalating through warn, gag, mute, kick and ban. Acts through dot-moderation when it is installed and through the server's own session state when it is not."
author="dot"
version="0.1.0"
script="dot_server_security_plugin.gd"
